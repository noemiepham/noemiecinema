

const initialState = {
  data: undefined,
  loading: false,
  success: false,
  error: undefined,
};

const searchReducer = (state = initialState, action) => {
  switch (action) {
    case Types.:
      
      break;
  
    default:
      break;
  }
};

export default searchReducer;
